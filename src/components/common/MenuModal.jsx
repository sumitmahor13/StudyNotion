import React from 'react'

const MenuModal = ({setLinkModal}) => {
  return (
    <div className=' absolute top-10 text-white' onClick={setLinkModal(false)}>
      Hiii
    </div>
  )
}

export default MenuModal
